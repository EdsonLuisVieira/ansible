<VirtualHost *:{{ porta }}>
    ServerAdmin {{ admin }}@{{ domain }}
    DocumentRoot {{ diretorio_index }}
    ServerName {{ domain }}
    ServerAlias {{ alias }}
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log common
</VirtualHost>
